[Unit]
Description=Daily cleanup of stale Secretary v2 rotated logs

[Timer]
OnCalendar=daily
# Run on next boot if the VPS was off when the timer should have fired.
Persistent=true
RandomizedDelaySec=1h

[Install]
WantedBy=timers.target
